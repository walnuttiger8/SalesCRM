<Window x:Class="Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Desktop.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Base/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="120 0">
            <Button x:Name="addCompanyButton" Style="{StaticResource inputButton}" Click="addCompanyButton_Click">Добавить компанию</Button>
            <Button x:Name="addCompanyRepresentativeButton" Style="{StaticResource inputButton}" Click="addCompanyRepresentativeButton_Click">Добавить лицо-представителя</Button>
            <Button x:Name="addContactButton" Style="{StaticResource inputButton}" Click="addContactButton_Click">Добавить взаимодействие</Button>
            <Button x:Name="showCompaniesButton" Style="{StaticResource inputButton}" Click="showCompaniesButton_Click">Компании</Button>
        </StackPanel>
    </Grid>
</Window>
